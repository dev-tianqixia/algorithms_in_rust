// Segment Tree is a data structure that effectively handles range query and range update in O(logn) time.
// Segement tree requires 4n vertices for working with an array of size n.
// leaf: a node is leaf if its corresponding segment covers only only value in the original array.
// 
